# Example environment variables for PostgreSQL access
# Copy to `.env` or export in your environment before running scripts

export PGHOST=localhost
export PGPORT=5432
export PGUSER=postgres
export PGPASSWORD=your_password_here
export PGDATABASE=geesp
export RETENTION_DAYS=7
