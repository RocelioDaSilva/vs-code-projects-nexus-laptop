# DEPRECATED — Dead Code

These files (`api.py`, `api_server.py`) are **not imported anywhere** in the live
codebase. The production API is at `backend/api/api.py`.

These stubs were originally created for test compatibility during an earlier phase.
They can be safely deleted once the ARCHIVE tests are also removed.

Kept for now to avoid breaking the frozen `tests_phase5_frozen/` archive.
